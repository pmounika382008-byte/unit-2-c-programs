//C Program to Count Number of Elements in an Array
#include <stdio.h>
int main() {
    int arr[] = {10, 20, 30, 40, 50, 60};

    int count = sizeof(arr) / sizeof(arr[0]);

    printf("The number of elements in the array is: %d\n", count);

    return 0;
}
